# Core dependencies
torch>=2.0.0
transformers>=4.30.0
tokenizers>=0.13.0
accelerate>=0.20.0

# Model conversion libraries
onnx>=1.14.0
onnxruntime>=1.15.0

# Optional: GGUF conversion
llama-cpp-python>=0.2.0

# Optional: MLX for Apple Silicon
mlx>=0.0.8

# Optional: Quantization libraries
auto-gptq>=0.4.0  # ⚠️ 仅支持Linux+NVIDIA显卡环境，macOS/无CUDA环境下可忽略安装报错
awq>=0.1.0

# Configuration and utilities
pyyaml>=6.0
click>=8.0.0
rich>=13.0.0
tqdm>=4.65.0

# Optional: For better performance
sentencepiece>=0.1.99
protobuf>=3.20.0

# Development dependencies
pytest>=7.0.0
black>=23.0.0
flake8>=6.0.0

autoawq>=0.2.0    # ⚠️ 仅支持Linux+NVIDIA显卡环境，macOS/无CUDA环境下可忽略安装报错 