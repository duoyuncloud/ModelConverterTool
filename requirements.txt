# Core dependencies
torch>=2.0.0
transformers>=4.30.0
tokenizers>=0.13.0
accelerate>=0.20.0

# Model conversion libraries
onnx>=1.14.0
onnxruntime>=1.15.0

# Optional: GGUF conversion
llama-cpp-python>=0.2.0

# Quantization libraries (install after torch, optional)
gptqmodel==2.2.0  # 主要量化转换库
logbar
tokenicer
device_smi
threadpoolctl
Pillow
safetensors

# Configuration and utilities
pyyaml>=6.0
rich>=13.0.0
tqdm>=4.65.0

# Optional: For better performance
sentencepiece>=0.1.99
protobuf>=3.20.0
datasets>=2.0.0  # Required for quantization libraries

# Development dependencies
pytest>=7.0.0
black>=23.0.0
flake8>=6.0.0

# Optional/advanced quantization dependencies
optimum
peft
# triton  # OpenAI Triton，仅限部分Linux+Python3.8~3.10环境需要
lm-eval
evalplus
# intel-extension-for-pytorch  # 仅限Linux+Intel XPU/CPU优化环境需要
# bitblas  # 仅部分高级量化/推理开发者需要，普通用户可忽略

random_word>=1.0.8
numpy
typer 