import typer

def version():
    """
    [dim]Examples:
      modelconvert version
      modelconvert version  # (no arguments)[/dim]

    Show the tool version.
    """
    typer.echo("modelconvertertool version 1.0.0") 